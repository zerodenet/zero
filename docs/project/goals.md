# 项目目标

长期目标：

- 做成完整的代理工具
- 同时保留可复用的内核结构

阶段目标：

- `v0.0.x`：把代理主链路、本地可用性、云端最小可用性、基础观测和节点组能力做通
- `v0.1.x`：作为内核正式发布线，收紧配置、日志、错误、运行稳定性和控制面核心规范
- `v0.2.x`：内核继续领先推进下一组能力；面板开始对接上一版稳定内核
- `v1.0`：再谈接口稳定、客户端启动和长期可维护

判断项目有没有走对，主要看三件事：

- 终端工具能不能直接用
- 新协议能不能按模块加进去
- 新平台适配时会不会把核心层扯乱
