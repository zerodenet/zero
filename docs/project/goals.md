# 项目目标

长期目标：

- 做成完整的代理工具
- 同时保留可复用的内核结构

阶段目标：

- `v0.1.x`：把 TCP 代理主链路做通，验证分层没问题
- `v0.2.x`：再补协议、UDP、DNS 和管理面
- `v1.0`：再谈接口稳定和长期可维护

判断项目有没有走对，主要看三件事：

- 终端工具能不能直接用
- 新协议能不能按模块加进去
- 新平台适配时会不会把核心层扯乱
