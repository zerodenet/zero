# 项目定位

Zero 是项目名，不是某一个 crate。

当前仓库提供一个可运行的命令行代理程序，同时保持可复用内核结构。Zero 有两条线：

- 一个给终端用户直接用的代理工具
- 一套能被别的系统复用的代理内核

这两条线不冲突。工具本身就是内核的第一层落地。

当前优先把最小链路做稳：

- 配置能读
- 入站能接
- 路由能判
- 出站能走
- 日志和状态能看

内核能力需要持续收紧：

- 配置边界
- 模式和节点组
- 错误和日志质量
- 运行稳定性
