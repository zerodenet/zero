# v0.1.0

这个版本号保留给首次正式发布版。

在达到正式发布条件之前，不继续推进这条线，当前只维护 `v0.0.x`。

等下面这些条件基本成立后，再正式展开 `v0.1.0`：

- 本地使用方式稳定
- 云端节点使用方式稳定
- 日志、错误和配置边界收紧
- 观测和控制面核心规范明确
- `zero-api` 的最小长期模型明确
- 版本范围不再频繁变动

`v0.1.0` 的目标是内核正式发布，不是面板正式发布。面板按项目生命周期落后一版跟进，优先对接上一版已经稳定的内核能力。
